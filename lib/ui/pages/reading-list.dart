import 'package:flutter/material.dart';

import '../styles/app-style.dart';

class ReadingListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Reading List",style: welcomeFont,),
    );
  }
}